<!-- ============================================================
     告警角标
     ============================================================ -->
<view wx:if="{{count > 0}}" class="badge">
  {{count > 99 ? '99+' : count}}
</view>
